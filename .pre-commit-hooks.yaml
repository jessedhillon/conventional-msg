- id: conventional-msg
  name: conventional commit message
  description: "enforces a modified conventional commit format: type(area[,...])[!]: [{tag}] message"
  entry: conventional-msg
  language: python
  stages: [commit-msg]
